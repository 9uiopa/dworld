name: CI/CD for Docker Compose Project

on:
  push:
    branches:
      - deploy

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. 코드 체크아웃
    - name: Checkout code
      uses: actions/checkout@v3

    # 2. Java 빌드 및 테스트
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        distribution: 'corretto'
        java-version: '17'
    # gradlew 실행권한 부여
    - name: Grant execute permission for gradlew
      run: chmod +x ./gradlew
      
    - name: Build with Gradle
      run: ./gradlew build

    # 3. Docker 이미지 빌드
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/dworld:${{ github.run_number }} .
        docker push ${{ secrets.DOCKER_USERNAME }}/dworld:${{ github.run_number }}

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
    # 1. EC2 서버에 SSH 접속
    - name: Deploy to EC2
      uses: appleboy/ssh-action@v0.1.9
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.SSH_KEY }}
        port: ${{ secrets.SSH_PORT }}

        script: |
          export TAG=${{ github.run_number }}
          sed -i "s|image:.*|image: ${{ secrets.DOCKER_USERNAME }}/dworld:${TAG}|" docker-compose.yml
          docker-compose pull
          docker-compose up -d
